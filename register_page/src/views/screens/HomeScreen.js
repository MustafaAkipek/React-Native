import AsyncStorage from '@react-native-async-storage/async-storage'; // veri depolamak için kullanılıyor
import React from 'react'; // ana kütüphane
import {View} from 'react-native'; // uygulamanın arayüzünde kullanılan temel bir bileşen

const HomeScreen = ({navigation}) => { // prop(navigation) ile ekranlar arası gezinmeyi sağlayan bir navigasyon nesnesini temsil ediyor
    return <View></View>;
};

export default HomeScreen; // bileşeni başka dosyalarda da kullanmaya imkan sağlıyor